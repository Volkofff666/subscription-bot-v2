services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: subscription-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "9443:9443"
    volumes:
      - ./data:/app/data
